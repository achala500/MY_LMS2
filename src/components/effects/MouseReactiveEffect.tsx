'use client';

import React, { useEffect, useRef, useState } from 'react';
import { usePathname } from 'next/navigation';

/**
 * MouseReactiveEffect
 * Ultra-lightweight, zero-leak ambient cursor effect for desktop fine-pointer devices only.
 * Completely disabled on mobile and touch devices.
 * Uses event-driven requestAnimationFrame that completely sleeps when the cursor is idle.
 */
export const MouseReactiveEffect: React.FC = () => {
  return null;
};

export default MouseReactiveEffect;
